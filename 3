import numpy as np
import matplotlib.pyplot as plt

# Konstanta
a = 2  # percepatan tetap (m/s^2)
total_time = 100  # total waktu gerak (detik)

# Fungsi untuk menghitung posisi
def menghitung_posisi(waktu):
    return 0.5 * a * waktu**2
